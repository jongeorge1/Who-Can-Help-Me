<?xml version="1.0"?>
<configuration>
	<configSections>
		<section name="analyticsConfigurationSection" type="WhoCanHelpMe.Infrastructure.Analytics.Configuration.AnalyticsConfigurationSection"/>
	</configSections>
	<analyticsConfigurationSection xmlns="http://schemas.who-can-help.me/analytics/configuration">
		<!--
		This is just a minimal sample configuration file that shows how to declare
		the configuration sections.
		
		Because an XML Schema Definition (XSD) is generated for each configuration
		section, it should be trivial to edit these files because you have
		IntelliSense on the XML definition.
		-->
	</analyticsConfigurationSection>
</configuration>
